# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Python ops env (uv / podman-compose)
.venv
__pycache__/

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Doppler service token on disk (Pi / laptop) — never commit
**/doppler.token

# Rendered from docker/nginx.prod.conf.template (LAN_IP); do not commit.
docker/nginx.prod.rendered.conf

# Local upload storage (legacy / unused — files go to Cloudflare R2)
data/uploads/
