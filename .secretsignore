# Paths excluded from `npm run scan:secrets` (scripts/scan-secrets.mjs).
# One glob per line; `*` matches within a path segment, `**` across segments.
#
# Only add paths here that genuinely cannot hold credentials. To silence a
# single line instead, put `secret-scan:ignore` in a comment on that line.

# Documented env template — values are placeholders by definition.
.env.example
