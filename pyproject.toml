[project]
name = "management-ops"
version = "0.1.0"
description = "Ops tooling for production deploy (podman-compose via uv)"
requires-python = ">=3.10"
dependencies = [
  "podman-compose>=1.2.0",
]

[tool.uv]
package = false
