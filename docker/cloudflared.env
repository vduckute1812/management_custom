# Named Cloudflare Tunnel settings (safe to commit — no secrets).
# Credentials live in ~/.cloudflared/ and are never committed.

TUNNEL_NAME=mgmt-dntechx
TUNNEL_HOSTNAME=dntechx.com
TUNNEL_HOSTNAME_WWW=www.dntechx.com
TUNNEL_UPSTREAM=http://127.0.0.1:8080
APP_BASE_URL=https://dntechx.com
