---
description: Nuxt 3 conventions for this SPA — architecture, API, auth, UI feedback
globs: "**/*.{vue,ts}"
alwaysApply: false
---

# Nuxt 3 Project Standards

This app is a **client-only SPA** (`routeRules: { "/**": { ssr: false } }`). Prefer patterns that match the existing stack: Nuxt 3 + Tailwind 4 + Zod + Bearer auth.

## Architecture

* **`pages/` / `components/`**: UI only. Extract data, multi-step flows, and shared state into `composables/`.
* **`composables/`**: Vue/Nuxt context (`ref`, `useState`, `useRoute`, `apiFetch`). Always pass an explicit key to `useState('unique-key', factory)`.
* **`utils/`** (client) / pure helpers: deterministic functions only — no `useNuxtApp()`, `useRoute()`, or `useRuntimeConfig()` at module top level.
* **`server/api/`**: Thin handlers. Auth → validate → call `server/db` or `server/utils` → return JSON or `createError`.
* **`server/db/`**: SQL / domain persistence. **`server/utils/`**: auth, mailer, R2, shared server helpers.
* **`types/`**: Shared DTOs used by both client and server (e.g. `types/post.ts`).

## Data fetching & auth (client)

* Authenticated API calls: use `apiFetch` from `useApi()` — not raw `$fetch` (attaches Bearer, refresh-on-401).
* `$fetch` is OK only for unauthenticated auth endpoints (`login` / `signup` / `refresh`) or one-off public calls.
* Do not introduce Pinia unless necessary; follow existing composable stores (`useAuth`, `useTasks`, …).

## Nitro API handlers

```ts
// ✅ validate → auth → domain
const user = requireUser(event) // or requireAdmin(event)
const parsed = bodySchema.safeParse(await readBody(event))
if (!parsed.success) {
  throw createError({ statusCode: 400, statusMessage: "Invalid body" })
}
```

* Validate body/query with **Zod** (`safeParse` or `readValidatedBody` / `getValidatedQuery`).
* Use `requireUser` / `requireAdmin` from `~/server/utils/authContext` — do not re-parse JWT in each route.
* Prefer semantic status codes: `400` validation, `401` unauthenticated, `403` forbidden, `404` missing, `500` unexpected.
* Never log passwords, tokens, or raw Authorization headers.

## UI / UX (practical)

* Loading: use existing `SkeletonBlock` / `SkeletonList` (or matching pulse placeholders) — avoid blank flashes.
* Mutations: give immediate feedback (toast via `useToasts`, disabled/pending buttons). Optimistic UI + rollback for toggles (like/reaction) when already patterned that way.
* Risky subtrees: wrap with `<NuxtErrorBoundary>` when isolation matters (see feed).
* Images: set `width` / `height` (or fixed aspect) to limit CLS. No `@nuxt/image` in this project — native `<img>` / media is fine.
* Theme: respect `data-theme` + existing slate/brand utilities; do not invent a parallel token system (`bg-background`, etc.).
* Motion: 150–300ms; honor `prefers-reduced-motion`.
* a11y: visible labels, keyboard focus rings, meaningful `statusMessage` / inline errors on forms.
* Page titles: `useSeoMeta({ title })` on pages that need a distinct document title.

## Performance (SPA-aware)

* Lazy-load heavy / conditional UI: `<Lazy*>` or `defineAsyncComponent` for modals, charts, story viewer.
* Browser-only APIs (`window`, `localStorage`): safe in this SPA after mount; still avoid running them in shared modules imported by server code.
* Keep Chart.js and large editors out of the initial critical path when possible.

## i18n

* Use `$t` / `useI18n().t` for user-facing copy. Add keys to **all four** locale files under `i18n/locales/` (`en`, `vi`, `zh-CN`, `zh-TW`).
* Persist locale via `useSettings().locale`; `@nuxtjs/i18n` strategy is `no_prefix` (see `nuxt.config.ts` + `plugins/i18n-locale.client.ts`).
* Do **not** invent a parallel i18n stack or hardcode English UI strings in new UI.
* Prefer `STATUS_I18N_KEYS` / `ROLE_I18N_KEYS` / `PRIORITY_I18N_KEYS` from `~/types/task` with `t(...)` for status, role, and priority labels.
